name: Build and test
on: push

jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'corretto'
          cache: maven

      - name: 1. Compile
        run: mvn compile

      - name: 2. Test
        run: mvn test --batch-mode

      - name: 3. mkdir /staging
        run: mkdir staging

      - name: 4. Build Windows Artifact
        run: |
          mvn package -DskipTests -Pwindows
          mv target/*-with-dependencies.jar staging/aiandme-windows.jar

      - name: 5. Build Linux Artifact
        run: |
          mvn package -DskipTests -Plinux
          mv target/*-with-dependencies.jar staging/aiandme-linux.jar

      - name: 6. Build Mac Artifact
        run: |
          mvn package -DskipTests -Pmac
          mv target/*-with-dependencies.jar staging/aiandme-mac.jar

      - name: 7. Upload All Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: multiplatform-builds
          path: staging/